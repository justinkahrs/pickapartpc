// @flow
import * as React from 'react';
import CaseSelect from '../components/caseSelect';
import './build.css';

const Build = () => (
  <div>
    <h1>Build</h1>
    <CaseSelect />
  </div>
);
export default Build;
